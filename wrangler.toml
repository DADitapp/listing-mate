name = "listing-mate"
pages_build_output_dir = ".open-next/assets"
compatibility_date = "2025-02-14"
# Compatibility flags are managed via the Cloudflare Dashboard to avoid Git-integrated CI conflicts.
# Ensure 'nodejs_compat' is enabled in Settings > Functions > Compatibility Flags.
